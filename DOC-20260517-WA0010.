
# USHINDI MURHABAZI CHRISTOPHE 
# RAÏSSA RUTEBUKA 
# TP N°1 d'intelligence artificielle
# Bac2 génie minier/école des mines/UOB




# 1) FONCTIONS DE CALCUL 
#________________________

def addition(x, y):
    return x + y

def soustraction(x, y):
    return x - y

def multiplication(a, b):
    return x * y

def division(x, y):
    if y == 0:
        raise ValueError("Erreur : Division par zéro impossible.")
    return x / y

# 2) CALCULS SUR LES VECTEURS
#____________________________


def addition_vecteurs(w1, w2):
    if len(w1) != len(w2):
        raise ValueError("Les vecteurs doivent avoir la même taille.")
    
    # Utilisation d'une boucle pour additionner élément par élément
    resultat = []
    for i in range(len(w1)):
        resultat.append(w1[i] + w2[i])
    return resultat

def produit_scalaire(w1, w2):
    if len(w1) != len(w2):
        raise ValueError("Les vecteurs doivent avoir la même taille.")
    
    somme = 0
    for i in range(len(w1)):
        somme = somme + v1[i] * v2[i]
    return somme



# 3) CALCULS SUR LES MATRICES
#____________________________


def addition_matrices(m1, m2):
    # Vérification des dimensions (lignes et colonnes)
    if len(m1) != len(m2) or len(m1[0]) != len(m2[0]):
        raise ValueError("Les matrices doivent avoir les mêmes dimensions.")
    
    lignes = len(m1)
    colonnes = len(m1[0])
    
    # Initialisation d'une matrice vide de même taille
    resultat = [[0 for _ in range(colonnes)] for _ in range(lignes)]
    
    for i in range(lignes):
        for j in range(colonnes):
            resultat[i][j] = m1[i][j] + m2[i][j]
    return resultat

def multiplication_matrices(P, T):
    # Nombre de colonnes de P doit être égal au nombre de lignes de T
    if len(P[0]) != len(T):
        raise ValueError("Le nombre de colonnes de A doit être égal au nombre de lignes de B.")
    
    lignes_P = len(P)
    colonnes_P = len(P[0])
    colonnes_T = len(T[0])
    
    # Initialisation de la matrice résultat (taille : lignes_P x colonnes_T)
    resultat = [[0 for _ in range(colonnes_T)] for _ in range(lignes_P)]
    
    # Algorithme de multiplication de matrices
    for i in range(lignes_P):
        for j in range(colonnes_T):
            somme = 0
            for k in range(colonnes_A):
                somme += P[i][k] * T[k][j]
            resultat[i][j] = somme
            
    return resultat



# ZONE DE TEST (EXEMPLES D'UTILISATION)
#______________________________________

if __name__ == "__main__":
    print("--- Tests opérations de base ---")
    print(f"Addition (5 + 3) : {addition(5, 3)}")
    print(f"Division (10 / 2) : {division(10, 2)}")
    
    print("\n--- Tests Vecteurs ---")
    vec1 = [1, 2, 3]
    vec2 = [4, 5, 6]
    print(f"Vecteur 1 : {vec1}")
    print(f"Vecteur 2 : {vec2}")
    print(f"Addition vecteurs : {addition_vecteurs(vec1, vec2)}")
    print(f"Produit scalaire : {produit_scalaire(vec1, vec2)}")
    
    print("\n--- Tests Matrices ---")
    matA = [
        [1, 2],
        [3, 4]
    ]
    matB = [
        [5, 6],
        [7, 8]
    ]
    print("Matrice A :", matA)
    print("Matrice B :", matB)
    print("Addition Matrices :", addition_matrices(matA, matB))
    print("Multiplication Matrices :", multiplication_matrices(matA, matB))
